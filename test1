
This is a test for test1
echo "# withDongHyunPark" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/sangropark/withDongHyunPark.git
git push -u origin master

echo "# withDongHyunPark" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/sangropark/withDongHyunPark.git
git push -u origin master

import numpy as np
import matplotlib.pyplot as plt
%matplotlib notebook
x=np.arange(0, 10, 0.1)
y=np.sin(x)
y2=np.cos(x)


fig=plt.figure()

ax=fig.add_subplot(111)
ax.plot(x,y)
ax.plot(x,y2)
plt.show()
